/**
 * UI Types
 * Centralized TypeScript type definitions for UI state
 */

export enum ViewMode {
    GLOBE = 'globe',
    GRID = 'grid',
    LIST = 'list',
}

export type Tab = 'discovery' | 'myevents' | 'calendar' | 'alerts' | 'settings';
